This example shows how to set up IRQ-handlers. Here it is shown how to process receive interrupt from the miniUSB UART and put the received data in a buffer (a ring buffer, specifically). The buffer is read intermittedly.
